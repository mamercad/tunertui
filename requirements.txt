# TunerTUI Dependencies
# Install with: pip install -r requirements.txt
# Or with uv: uv pip install -r requirements.txt
# Or with uv.lock: uv sync

textual>=0.30.0
numpy>=1.23.0
sounddevice>=0.4.5
scipy>=1.9.0
